#ifndef loading_H
#define loading_H

void gotoxy(int X, int y); //fungsi yang digunakan untuk memindahkan cursor (garis vertikal atau horizontal yang berkedip-kedip)
						  // di layar ke posisi yang diinginkan
void loading_screen(); //tampilan loading

#endif
